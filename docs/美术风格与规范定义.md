# 美术规范总稿

本文件为当前项目唯一生效的美术规范主文档。原有分散的风格稿、标准件任务单、资源替换清单、缺口清单、秘境专项说明、返修说明，均已并入本文，不再单独维护。

适用范围：

- `assets/scripts/GrottoExpeditionDemo.ts` 当前已实现的首页、洞天、商城、法器、角色、秘境、任务、功法、灵宠、抽奖、确认框等界面
- `assets/resources/art/generated/xianxia/` 当前运行时加载资源目录
- 后续新增的仙侠 UI、角色展示、秘境地图、状态章印、稀有度资源

---

## 一、项目定位与风格规范

### 1. 项目定位

- 题材：修仙 / 仙侠 / 洞府养成 / 秘境探险
- 核心体验：洞府经营成长 + 下百层秘境搜打撤 + 法器 / 功法 / 灵宠养成
- 当前目标：用一套统一的 2D 修仙像素 UI 包装，替换程序色块和临时占位图

### 2. 风格结论

统一方向：深色像素风修仙 UI + 中式机关铜框 + 低饱和石板面板 + 小尺寸像素灵物图标。

### 3. 风格关键词

- 像素修仙
- 深蓝灰石板
- 铜金包边
- 古典机关
- 小尺寸精灵图标
- 昏暗洞府火光
- 中式符纹
- 厚重但清晰

### 4. 禁止项

- 不做赛博修仙
- 不做废土脏污风
- 不做西幻恶魔风
- 不做重血腥恐怖风
- 不做 Q 版轻喜剧路线
- 不做现代玻璃拟物或科幻面板
- 不做轻水墨大写意 UI
- 不做过度柔光的手游卡绘图标

---

## 二、视觉基调与配色规范

### 1. 主色体系

- 主色：深蓝灰、石墨灰、暗铜金、低饱和青蓝
- 文本高亮：暖金、米白
- 功能高亮：青蓝、琥珀橙、翠绿
- 警示点缀：砖红、暗朱砂

### 2. 材质与纹理

- 深色石板纹理
- 铜质包边
- 机关榫卯感角饰
- 像素火光
- 低频符纹与古印记
- 小范围灵气像素辉光

### 3. 亮度与对比规则

- 大面板重阅读，装饰服从信息
- 高亮只服务可点击、可领取、可升级、稀有奖励
- 禁用态必须明显失活，不能只降少量亮度
- 小尺寸控件避免厚边框和脏纹理，优先保证轮廓识别
- 图标优先像素轮廓识别，不追求平滑厚涂质感
- 小 icon 不依赖大面积雾效和写意笔触，避免缩小后发糊

---

## 三、角色、场景与世界表现规范

### 1. 主角与生物

- 主角为像素化仙侠修士形象，长袍、束发、法冠、立姿清晰
- 灵宠偏灵兽，不偏萌宠，不做玩具宠物
- 妖兽、邪修、镇守者都必须保持东方仙侠根基，并适配小尺寸像素表现

### 2. 核心场景要求

- 洞府：安定、可经营、可修行，强调静室、灵脉、丹炉、阵法
- 秘境：洞窟、地宫、古遗迹、深井向下延伸，强调层层下探感
- 功法阁 / 炼器台 / 灵宠栖所 / 渡劫台：沿用同一世界材质和色彩体系

### 3. 角色展示区要求

- 角色展示允许使用像素法阵、底环、火烛、符印、微光等装饰资源
- 装饰必须服务角色聚焦，不能遮挡角色主体
- 角色页主视觉优先保证人物识别，其次再叠加法阵和像素灵光

---

## 四、UI 标准件规范

本项目不再允许逐页重新发明一套皮肤，统一按标准件生产和复用。

### 1. P0 必须具备的标准件

- 主面板底板
- 次级内容面板
- 次级卡片底板
- 横向列表卡底
- 主按钮 S / M / L
- 次按钮 S / M
- 页签按钮默认态 / 选中态
- 标题装饰条
- 关闭按钮
- 进度条底图 / 填充条
- 遮罩底图

### 2. 尺寸基准

- 主面板底板：1304 x 1536
- 次级内容面板：1240 x 440
- 次级卡片底板：620 x 320
- 横向列表卡底：1100 x 160
- 主按钮：288 x 84 / 312 x 96 / 360 x 104
- 次按钮：220 x 72 / 272 x 96
- 页签按钮：224 x 112

### 3. 状态要求

- 默认态
- 选中态
- 按下态
- 禁用态
- 可领取 / 可升级高亮态
- 已完成 / 已装备 / 已售罄态

### 4. 复用规则

- 同层级控件只保留一套视觉语言
- 优先支持九宫或等比缩放
- 不允许依赖逐页重画才能适配
- 标准件先覆盖 80% 页面，再做少量玩法专项件

---

## 五、资源命名、目录与交付清单规范

### 1. 当前唯一运行时目录

统一放置于：`assets/resources/art/generated/xianxia/`

后续若迁正式目录，也必须在程序改路径前统一迁移，不允许文档和代码长期并存两套路径口径。

### 2. 命名规则

- 全部使用小写英文 + 中横线
- 前缀按类别区分：`ui-`、`btn-`、`icon-`、`rarity-`、`grotto-`、`dungeon-`
- 状态资源要直接写进文件名，例如：`icon-badge-done`、`icon-badge-locked`
- 稀有度资源要显式写颜色档位，例如：`rarity-frame-blue`

### 3. 当前项目首批必须覆盖的资源组

- 背景组：`grotto-bg`、`dungeon-qi-bg`、`dungeon-deep-bg`
- 角色组：`protagonist-seated`、角色展示装饰组
- 基础 UI 组：`ui-panel-bg`、主按钮、次按钮、页签、标题装饰、遮罩
- 基础数值 icon 组：灵石、秘晶、修为、气血、灵力、行动力、徽记
- 状态章印组：claimable / done / equipped / locked / soldout / upgradable
- 稀有度组：frame / topbar / corner / glow
- 秘境节点组：tile icon、卡牌背板、direction marker、地图背板、路径装饰
- 抽奖与确认框组：confirm panel、confirm buttons、lottery wheel、pointer、center seal、sector、result seal

### 4. 资源状态维护规则

- 以后不再单独维护“缺口清单”文档
- 缺资源、待返修、待接程序，一律回写到本章对应资源组下
- 资源状态只允许使用四种：`待制作`、`待返修`、`可接程序`、`已接入`

### 5. 当前已知待返修资源

以下资源当前仍按整张大画布导出，状态定义为 `待返修`：

- `ui-tile-opened`
- `ui-tile-unopened`
- `ui-tile-reward-clue`
- `ui-tile-danger-clue`
- `rarity-topbar-*`
- `rarity-corner-*`
- `rarity-chest-glow`
- `ui-confirm-title`
- `ui-lottery-border`
- `ui-lottery-sector`
- `ui-lottery-result-seal`
- `icon-lottery-benefit-*`
- `icon-lottery-harm-*`

---

## 六、导出、切片与程序导入规范

这一章是强制门槛，任何资源不满足以下要求，程序不得接入。

### 1. 导出原则

- 每个资源单独导出
- 必须是真实透明 png
- 主体必须占据有效画面的大部分面积
- 透明边距尽量紧
- 不允许把整张大场景画布当小控件图使用

### 2. 明确禁止

- 不要导出 1376 x 768 的整张大画布给按钮、角标、顶部条、提示 icon 使用
- 不要把多个组件塞进同一张图后交给程序裁切
- 不要导出 `hasAlpha: false` 的伪透明组件图
- 不要把整屏构图当成稀有度条、抽奖扇区、确认框标题条

### 3. 推荐尺寸

- 小 icon：64 x 64 / 80 x 80 / 128 x 128
- 状态章印：128 x 128 / 160 x 160
- 稀有度顶部条：约 100 x 28
- 稀有度角标：约 36 x 36
- 掉落辉光：约 120 x 120 / 140 x 140
- 秘境格子底板：约 180 x 236
- 线索 icon：约 80 x 80 到 120 x 120
- 确认框标题条：约 180 x 40
- 抽奖正负收益 icon：约 64 x 64 / 80 x 80

### 4. Cocos 导入要求

- 放在 `resources` 下，保证运行时可加载
- 导入后应为可用透明资源，不接受错误导成纯 `texture` 且无 alpha 的小组件图
- 可拉伸的大底板和按钮优先预留九宫切分空间
- 新资源接入前必须先核对 `.meta` 是否符合预期

---

## 七、页面专项表现规范

### 1. 首页与局外页

- 首页 / 角色页：静修感、洞府感、主角聚焦
- 商城：云游商坊、珍品柜感，商品卡优先可读性
- 法器 / 功法 / 灵宠：先保证列表结构统一，再做少量专题装饰
- 任务 / 确认框：优先清晰、明确、层级稳定，不追求过重装饰

### 2. 秘境页面

秘境是唯一允许明显区别于常规面板布局的专项页面，但仍必须遵守整体风格。

- 结构：上方状态区 + 中部地图位图区 + 下方操作区
- 地图核心：每层 1 到 3 张卡牌节点，形成向下延伸路线
- 卡牌结构：顶部类型签、中央主图区域、底部提示区
- 方向提示：每张卡牌正上方 45 度位置放独立 marker
- 背景气质：洞窟、地宫、古遗迹、深井，不做平铺房间选择界面
- 路径装饰：服务导视，不得过花

### 3. 秘境节点类型

- 灵植
- 灵石
- 珍材
- 妖兽
- 陷阱
- 机缘
- 邪修
- 镇守 / Boss
- 未探明随机

### 4. 稀有度与状态表达

- 稀有度先靠边框，再辅以顶部条或角标
- 状态先靠章印，再辅以文字
- 不允许只靠纯文字描述“已完成”“已装备”“可领取”

---

## 八、验收、替换与文档维护规范

### 1. 美术验收口径

- 缩小后仍能看清层级和边框
- 不能抢正文信息
- 不出现赛博、废土、西幻偏题元素
- 与现有仙侠 UI 世界观一致
- 小组件不出现被压缩成噪点或整屏脏底的问题

### 2. 程序接入口径

- 资源先过本规范第六章检查，再决定是否接代码
- 高风险资源先局部替换，不做全局换皮
- 接入后必须检查排版、可用区域、缩放和清晰度
- 不能为了“全部换上”牺牲布局和完成度

### 3. 文档维护规则

- 本文件是唯一主规范
- 不再新建平行的“替换清单”“缺口清单”“返修说明”文档
- 若后续出现专项需求，先补到对应章节；只有内容明显超出主文档承载时，才允许新增专项附录
- 新增附录必须在本文顶部说明用途和边界，避免再次分裂事实源

### 4. 当前执行结论

- 保留并继续使用已稳定接入的低风险资源：状态章印、秘境 tile icon、稀有度 frame、角色装饰
- 对整张大画布型资源，一律按 `待返修` 处理
- 后续若返修完成，优先补稀有度 topbar / corner、秘境 opened / unopened、reward / danger clue，再补抽奖与确认框附加件